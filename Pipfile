[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
streamlit = ">=1.47.0"
pandas = ">=1.5.0"
plotly = ">=5.15.0"
numpy = ">=1.24.0"
jinja2 = ">=3.1.0"
pytest = ">=7.0.0"
pytest-mock = ">=3.10.0"
psutil = ">=5.9.0"
reportlab = ">=4.0.0"

[dev-packages]

[requires]
python_version = "3.11"
python_full_version = "3.11.9"
